# Self-service overlay — adds web dashboard UI (nginx + React SPA).
#
# Recommended usage:
#   docker compose -f docker-compose.yml -f docker-compose.postgres.yml -f docker-compose.selfservice.yml up -d --build
#
# Dashboard: http://localhost:3000

services:
  argoclaw-ui:
    image: ghcr.io/nextlevelbuilder/argoclaw-web:latest
    build:
      context: ./ui/web
      dockerfile: Dockerfile
    ports:
      - "${ARGOCLAW_UI_PORT:-3000}:80"
    networks:
      - default
      - shared
    depends_on:
      - argoclaw
    restart: unless-stopped
